<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="CopilotDiffPersistence">
    <option name="pendingDiffs">
      <map>
        <entry key="/job (snapshot).sh">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="/job (snapshot).sh" />
              <option name="originalContent" value="#!/bin/bash&#10;#SBATCH --job-name=pde_training&#10;#SBATCH --nodes=1&#10;#SBATCH --ntasks=1&#10;#SBATCH --gpus=1&#10;#SBATCH --mem=32GB&#10;#SBATCH --time=08:00:00&#10;#SBATCH --output=logs/train_%j.log&#10;#SBATCH --error=logs/train_%j.err&#10;&#10;# ============================================================================&#10;# PDE Training Job Script Template&#10;# ============================================================================&#10;# This is a template SLURM job script for training neural network models.&#10;# Modify the environment loading section according to your cluster setup.&#10;# ============================================================================&#10;&#10;# Create logs directory if it doesn't exist&#10;mkdir -p logs&#10;&#10;# ============================================================================&#10;# ENVIRONMENT SETUP - CUSTOMIZE THIS SECTION FOR YOUR CLUSTER&#10;# ============================================================================&#10;# Uncomment and modify one of the following options based on your environment:&#10;&#10;# Option 1: Using Conda (recommended)&#10;# source /path/to/conda/etc/profile.d/conda.sh&#10;# conda activate your_env_name&#10;&#10;# Option 2: Using module system&#10;# module load python/3.11&#10;# module load cuda/12.1&#10;# module load cudnn/8.x&#10;&#10;# Option 3: Using virtualenv&#10;# source /path/to/venv/bin/activate&#10;&#10;# Option 4: Direct Python path (if system Python is used)&#10;# export PATH=/usr/local/python3.11/bin:$PATH&#10;&#10;# ============================================================================&#10;# VERIFY ENVIRONMENT&#10;# ============================================================================&#10;echo &quot;Job started at $(date)&quot;&#10;echo &quot;Running on $(hostname)&quot;&#10;echo &quot;Python executable: $(which python)&quot;&#10;python --version&#10;python -c &quot;import torch; print(f'PyTorch version: {torch.__version__}'); print(f'CUDA available: {torch.cuda.is_available()}')&quot;&#10;&#10;# ============================================================================&#10;# TRAINING COMMANDS - CHOOSE ONE&#10;# ============================================================================&#10;&#10;cd &quot;$(dirname &quot;$0&quot;)&quot; || exit 1&#10;&#10;# Command 1: Train specific rho values with default data location&#10;# python -m train rho 256 266 276&#10;&#10;# Command 2: Train specific dataset with all rho values&#10;# python -m train dataset data_001&#10;&#10;# Command 3: Train specific rho values for a dataset&#10;# python -m train rho 256 266 --dataset data_001&#10;&#10;# Command 4: Train with custom config&#10;# python -m train rho 256 266 276 --config train/custom_config.txt&#10;&#10;# Command 5: Multi-dataset training (sequential)&#10;# python -m train dataset data_001&#10;# python -m train dataset data_002&#10;&#10;# Example: Train default rho values (uncomment to use)&#10;python -m train rho 256 266 276&#10;&#10;# ============================================================================&#10;# JOB COMPLETION&#10;# ============================================================================&#10;echo &quot;Job completed at $(date)&quot;&#10;exit 0&#10;&#10;" />
              <option name="updatedContent" value="#!/bin/bash&#10;#SBATCH --job-name=pde_training&#10;#SBATCH --nodes=1&#10;#SBATCH --ntasks=1&#10;#SBATCH --partition=gpu&#10;#SBATCH --gpus=1&#10;#SBATCH --mem=32GB&#10;#SBATCH --time=08:00:00&#10;#SBATCH --output=logs/train_%j.log&#10;#SBATCH --error=logs/train_%j.err&#10;&#10;# ============================================================================&#10;# PDE Training Job Script for 北师港浸大 HPC Cluster&#10;# 基于 HPC 使用手册 V2026313&#10;# ============================================================================&#10;# GPU队列说明：&#10;#   - hpcgpu01: 4x V100 GPU&#10;#   - hpcgpu04, hpcgpu05: 4x A100 GPU&#10;# CPU队列说明：&#10;#   - 6台机器，每台2颗Intel Gold 5320 CPU，52个核心，256GB内存&#10;# ============================================================================&#10;&#10;mkdir -p logs&#10;&#10;# ============================================================================&#10;# ENVIRONMENT SETUP - 北师港浸大 HPC 环境配置&#10;# ============================================================================&#10;echo &quot;==========================================&quot;&#10;echo &quot;Job started at $(date)&quot;&#10;echo &quot;Running on $(hostname)&quot;&#10;echo &quot;Job ID: $SLURM_JOB_ID&quot;&#10;echo &quot;Allocated GPU(s): $CUDA_VISIBLE_DEVICES&quot;&#10;echo &quot;==========================================&quot;&#10;&#10;# 激活 Conda 环境（根据实际修改环境名称）&#10;source ~/.bashrc&#10;conda activate jq&#10;&#10;# 验证环境&#10;echo &quot;Python: $(which python)&quot;&#10;python --version&#10;&#10;echo &quot;PyTorch:&quot;&#10;python -c &quot;import torch; print(f'  Version: {torch.__version__}'); print(f'  CUDA available: {torch.cuda.is_available()}'); print(f'  GPU count: {torch.cuda.device_count()}')&quot; 2&gt;/dev/null || echo &quot;  PyTorch not available&quot;&#10;&#10;echo &quot;==========================================&quot;&#10;&#10;# ============================================================================&#10;# TRAINING COMMANDS - 选择一个命令取消注释运行&#10;# ============================================================================&#10;&#10;cd &quot;$(dirname &quot;$0&quot;)&quot; || exit 1&#10;&#10;# 命令1: 训练默认数据的三个rho值&#10;# python -m train rho 256 266 276&#10;&#10;# 命令2: 训练特定数据集的所有rho值&#10;# python -m train dataset data_001&#10;&#10;# 命令3: 为特定数据集训练特定rho值&#10;# python -m train rho 256 266 --dataset data_001&#10;&#10;# 命令4: 多数据集顺序训练&#10;# python -m train dataset data_001&#10;# python -m train dataset data_002&#10;&#10;# 默认：训练默认数据的三个rho值&#10;python -m train rho 256 266 276&#10;&#10;# ============================================================================&#10;# JOB COMPLETION&#10;# ============================================================================&#10;echo &quot;==========================================&quot;&#10;echo &quot;Job completed at $(date)&quot;&#10;echo &quot;Job ID: $SLURM_JOB_ID&quot;&#10;echo &quot;==========================================&quot;&#10;exit 0&#10;&#10;" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/FINAL_SUMMARY.md">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/FINAL_SUMMARY.md" />
              <option name="updatedContent" value="# 最终版交付说明&#10;&#10;## 1. 已实现内容&#10;- 新增 `dataset` 命令：`python -m train dataset &lt;dataset_id&gt;`&#10;- 增强 `rho` 命令：支持 `--dataset` 参数&#10;- 新增 early stopping 汇总：在模型输出目录生成 `stop_summary.txt`&#10;- 保持向后兼容：`python -m train rho 256 266 276` 仍可用&#10;&#10;## 2. 命令用法&#10;```bash&#10;python -m train dataset data_001&#10;python -m train dataset data_001 --rho 256 266&#10;python -m train rho 256 266 276&#10;python -m train rho 256 --dataset data_001&#10;python -m train rho 256 266 --dataset data_001 --config train/config.txt&#10;```&#10;&#10;## 3. 路径与输出规则&#10;- `dataset` 模式：&#10;  - 数据读取：`data/{dataset_id}/train_rho*.h5`&#10;  - 模型输出：`model/model_{dataset_id}/`&#10;- `rho` 模式（不带 `--dataset`）：&#10;  - 数据读取：`data/train_rho*.h5`&#10;  - 模型输出：`model/`&#10;- `rho` 模式（带 `--dataset`）：&#10;  - 数据读取：`data/{dataset_id}/train_rho*.h5`&#10;  - 模型输出：`model/model_{dataset_id}/`&#10;&#10;## 4. 关键输出文件&#10;- `model_rho*.pth`：模型权重&#10;- `zscore_stats_*.csv`：标准化统计&#10;- `stop_summary.txt`：early stopping 汇总，例如：&#10;&#10;```text&#10;[rho=256] Early stopping at epoch 34: LR=1.500e-04 | Train: ... | Val: ... | Test: ...&#10;```&#10;&#10;## 5. 兼容性与结论&#10;- 对现有训练流程无破坏性改动&#10;- 原有 `rho` 命令保持可用&#10;- 新增能力已覆盖多数据集训练与停止信息汇总&#10;&#10;---&#10;如需继续精简到“只保留命令与路径两节”，可以在此文件基础上再压缩一版。" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/train/__main__.py">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/train/__main__.py" />
              <option name="originalContent" value="from __future__ import annotations&#10;&#10;import argparse&#10;from pathlib import Path&#10;import sys&#10;&#10;import torch&#10;&#10;PROJECT_ROOT = Path(__file__).resolve().parent.parent&#10;if str(PROJECT_ROOT) not in sys.path:&#10;    sys.path.insert(0, str(PROJECT_ROOT))&#10;&#10;from project_runtime import cleanup_bytecode_caches, disable_bytecode_cache&#10;&#10;disable_bytecode_cache()&#10;&#10;if __package__ in {None, &quot;&quot;}:&#10;    from train.config import load_training_config&#10;    from train.data_save import (&#10;        append_log_line,&#10;        create_result_output_path,&#10;        create_training_log_output_path,&#10;        save_results_to_txt,&#10;        save_zscore_stats,&#10;    )&#10;    from train.dataloader import build_dataloaders_from_h5&#10;    from train.model_structure import build_model_for_rho&#10;    from train.train import fit_regression_model, set_all_seeds&#10;else:&#10;    from .config import load_training_config&#10;    from .data_save import (&#10;        append_log_line,&#10;        create_result_output_path,&#10;        create_training_log_output_path,&#10;        save_results_to_txt,&#10;        save_zscore_stats,&#10;    )&#10;    from .dataloader import build_dataloaders_from_h5&#10;    from .model_structure import build_model_for_rho&#10;    from .train import fit_regression_model, set_all_seeds&#10;&#10;&#10;def main() -&gt; None:&#10;    args = parse_args()&#10;    project_root = Path(__file__).resolve().parent.parent&#10;    config = load_training_config(args.config)&#10;    device = torch.device(&quot;cuda&quot; if torch.cuda.is_available() else &quot;cpu&quot;)&#10;&#10;    model_dir = project_root / &quot;model&quot;&#10;    results_dir = project_root / &quot;train&quot; / &quot;results&quot;&#10;    training_log_path = create_training_log_output_path(results_dir)&#10;    result_rows: list[dict[str, object]] = []&#10;&#10;    def emit(message: str) -&gt; None:&#10;        print(message)&#10;        append_log_line(training_log_path, message)&#10;&#10;    for rho in args.rho_values:&#10;        set_all_seeds(config.training.seed)&#10;        data_path = project_root / &quot;data&quot; / f&quot;train_rho{rho}.h5&quot;&#10;        checkpoint_path = model_dir / f&quot;model_rho{rho}.pth&quot;&#10;&#10;        emit(&quot;&quot;)&#10;        emit(&quot;=&quot; * 72)&#10;        emit(f&quot;Starting training for rho={rho} on device={device}&quot;)&#10;        emit(&quot;=&quot; * 72)&#10;&#10;        train_loader, val_loader, test_loader, meta = build_dataloaders_from_h5(&#10;            data_path,&#10;            batch_size=config.training.batch_size,&#10;            seed=config.training.seed,&#10;            num_workers=0,&#10;        )&#10;        emit(&#10;            f&quot;[rho={rho}] Data split | total={meta['N_total']} train={meta['N_train']} &quot;&#10;            f&quot;val={meta['N_val']} test={meta['N_test']} batch_size={meta['batch_size']}&quot;&#10;        )&#10;        if meta[&quot;zero_sigma_features&quot;]:&#10;            emit(&#10;                f&quot;[rho={rho}] Warning: {meta['zero_sigma_features']} feature(s) had zero std &quot;&#10;                &quot;and were kept at sigma=1.0 to avoid division by zero.&quot;&#10;            )&#10;&#10;        model = build_model_for_rho(rho, config.model)&#10;        save_zscore_stats(rho, meta[&quot;mu&quot;], meta[&quot;sigma&quot;], model_dir)&#10;&#10;        result = fit_regression_model(&#10;            rho,&#10;            model,&#10;            train_loader,&#10;            val_loader,&#10;            test_loader,&#10;            optimizer_config=config.optimizer,&#10;            training_config=config.training,&#10;            device=device,&#10;            save_path=checkpoint_path,&#10;            log_message=lambda message: append_log_line(training_log_path, message),&#10;        )&#10;        result_rows.append(result)&#10;&#10;    output_path = create_result_output_path(results_dir)&#10;    table = save_results_to_txt(result_rows, output_path)&#10;    emit(&quot;&quot;)&#10;    emit(&quot;Final summary:&quot;)&#10;    for line in table.splitlines():&#10;        emit(line)&#10;    emit(&quot;&quot;)&#10;    emit(f&quot;Final summary saved to {output_path}&quot;)&#10;    emit(f&quot;Training log saved to {training_log_path}&quot;)&#10;&#10;&#10;def parse_args() -&gt; argparse.Namespace:&#10;    parser = argparse.ArgumentParser(description=&quot;Train rho-specific curvature MLP models.&quot;)&#10;    subparsers = parser.add_subparsers(dest=&quot;command&quot;, required=True)&#10;&#10;    rho_parser = subparsers.add_parser(&quot;rho&quot;, help=&quot;Train one or more rho-specific models.&quot;)&#10;    rho_parser.add_argument(&quot;rho_values&quot;, nargs=&quot;+&quot;, type=int, help=&quot;rho values to train, e.g. 256 266 276&quot;)&#10;    rho_parser.add_argument(&#10;        &quot;--config&quot;,&#10;        default=str(Path(__file__).resolve().with_name(&quot;config.txt&quot;)),&#10;        help=&quot;Path to the training config file.&quot;,&#10;    )&#10;    return parser.parse_args()&#10;&#10;&#10;if __name__ == &quot;__main__&quot;:&#10;    try:&#10;        main()&#10;    finally:&#10;        cleanup_bytecode_caches(PROJECT_ROOT)&#10;" />
              <option name="updatedContent" value="from __future__ import annotations&#10;&#10;import argparse&#10;from pathlib import Path&#10;import sys&#10;&#10;import torch&#10;&#10;PROJECT_ROOT = Path(__file__).resolve().parent.parent&#10;if str(PROJECT_ROOT) not in sys.path:&#10;    sys.path.insert(0, str(PROJECT_ROOT))&#10;&#10;from project_runtime import cleanup_bytecode_caches, disable_bytecode_cache&#10;&#10;disable_bytecode_cache()&#10;&#10;if __package__ in {None, &quot;&quot;}:&#10;    from train.config import load_training_config&#10;    from train.data_save import (&#10;        append_log_line,&#10;        create_result_output_path,&#10;        create_training_log_output_path,&#10;        save_results_to_txt,&#10;        save_zscore_stats,&#10;    )&#10;    from train.dataloader import build_dataloaders_from_h5&#10;    from train.model_structure import build_model_for_rho&#10;    from train.train import fit_regression_model, set_all_seeds&#10;else:&#10;    from .config import load_training_config&#10;    from .data_save import (&#10;        append_log_line,&#10;        create_result_output_path,&#10;        create_training_log_output_path,&#10;        save_results_to_txt,&#10;        save_zscore_stats,&#10;    )&#10;    from .dataloader import build_dataloaders_from_h5&#10;    from .model_structure import build_model_for_rho&#10;    from .train import fit_regression_model, set_all_seeds&#10;&#10;&#10;def main() -&gt; None:&#10;    args = parse_args()&#10;    project_root = Path(__file__).resolve().parent.parent&#10;    config = load_training_config(args.config)&#10;    device = torch.device(&quot;cuda&quot; if torch.cuda.is_available() else &quot;cpu&quot;)&#10;&#10;    # Determine model directory and data directory based on command&#10;    if args.command == &quot;dataset&quot;:&#10;        dataset_id = args.dataset_id&#10;        model_dir = project_root / &quot;model&quot; / f&quot;model_{dataset_id}&quot;&#10;        data_dir = project_root / &quot;data&quot; / dataset_id&#10;    else:  # &quot;rho&quot; command&#10;        if args.dataset:&#10;            dataset_id = args.dataset&#10;            model_dir = project_root / &quot;model&quot; / f&quot;model_{dataset_id}&quot;&#10;            data_dir = project_root / &quot;data&quot; / dataset_id&#10;        else:&#10;            model_dir = project_root / &quot;model&quot;&#10;            data_dir = project_root / &quot;data&quot;&#10;&#10;    results_dir = project_root / &quot;train&quot; / &quot;results&quot;&#10;    training_log_path = create_training_log_output_path(results_dir)&#10;    result_rows: list[dict[str, object]] = []&#10;    stop_info_list: list[str] = []&#10;&#10;    def emit(message: str) -&gt; None:&#10;        print(message)&#10;        append_log_line(training_log_path, message)&#10;&#10;    for rho in args.rho_values:&#10;        set_all_seeds(config.training.seed)&#10;        data_path = data_dir / f&quot;train_rho{rho}.h5&quot;&#10;        checkpoint_path = model_dir / f&quot;model_rho{rho}.pth&quot;&#10;&#10;        emit(&quot;&quot;)&#10;        emit(&quot;=&quot; * 72)&#10;        emit(f&quot;Starting training for rho={rho} on device={device}&quot;)&#10;        emit(&quot;=&quot; * 72)&#10;&#10;        train_loader, val_loader, test_loader, meta = build_dataloaders_from_h5(&#10;            data_path,&#10;            batch_size=config.training.batch_size,&#10;            seed=config.training.seed,&#10;            num_workers=0,&#10;        )&#10;        emit(&#10;            f&quot;[rho={rho}] Data split | total={meta['N_total']} train={meta['N_train']} &quot;&#10;            f&quot;val={meta['N_val']} test={meta['N_test']} batch_size={meta['batch_size']}&quot;&#10;        )&#10;        if meta[&quot;zero_sigma_features&quot;]:&#10;            emit(&#10;                f&quot;[rho={rho}] Warning: {meta['zero_sigma_features']} feature(s) had zero std &quot;&#10;                &quot;and were kept at sigma=1.0 to avoid division by zero.&quot;&#10;            )&#10;&#10;        model = build_model_for_rho(rho, config.model)&#10;        save_zscore_stats(rho, meta[&quot;mu&quot;], meta[&quot;sigma&quot;], model_dir)&#10;&#10;        result = fit_regression_model(&#10;            rho,&#10;            model,&#10;            train_loader,&#10;            val_loader,&#10;            test_loader,&#10;            optimizer_config=config.optimizer,&#10;            training_config=config.training,&#10;            device=device,&#10;            save_path=checkpoint_path,&#10;            log_message=lambda message: append_log_line(training_log_path, message),&#10;        )&#10;        result_rows.append(result)&#10;        &#10;        # Collect stop info for summary file&#10;        if result.get(&quot;stop_info&quot;):&#10;            stop_info_list.append(f&quot;[rho={rho}] {result['stop_info']}&quot;)&#10;&#10;    output_path = create_result_output_path(results_dir)&#10;    table = save_results_to_txt(result_rows, output_path)&#10;    &#10;    # Save stop_summary.txt in model_dir&#10;    if stop_info_list:&#10;        stop_summary_path = model_dir / &quot;stop_summary.txt&quot;&#10;        model_dir.mkdir(parents=True, exist_ok=True)&#10;        with open(stop_summary_path, &quot;w&quot;, encoding=&quot;utf-8&quot;) as f:&#10;            f.write(&quot;\n&quot;.join(stop_info_list) + &quot;\n&quot;)&#10;        emit(f&quot;Stop summary saved to {stop_summary_path}&quot;)&#10;    &#10;    emit(&quot;&quot;)&#10;    emit(&quot;Final summary:&quot;)&#10;    for line in table.splitlines():&#10;        emit(line)&#10;    emit(&quot;&quot;)&#10;    emit(f&quot;Final summary saved to {output_path}&quot;)&#10;    emit(f&quot;Training log saved to {training_log_path}&quot;)&#10;&#10;&#10;def parse_args() -&gt; argparse.Namespace:&#10;    parser = argparse.ArgumentParser(description=&quot;Train rho-specific curvature MLP models.&quot;)&#10;    subparsers = parser.add_subparsers(dest=&quot;command&quot;, required=True)&#10;&#10;    # &quot;dataset&quot; command - train all rho values for a specific dataset&#10;    dataset_parser = subparsers.add_parser(&quot;dataset&quot;, help=&quot;Train all rho-specific models for a dataset.&quot;)&#10;    dataset_parser.add_argument(&quot;dataset_id&quot;, type=str, help=&quot;Dataset ID (e.g., data_001, acute_276)&quot;)&#10;    dataset_parser.add_argument(&#10;        &quot;--rho&quot;,&#10;        nargs=&quot;+&quot;,&#10;        type=int,&#10;        default=[256, 266, 276],&#10;        help=&quot;rho values to train (default: 256 266 276)&quot;,&#10;    )&#10;    dataset_parser.add_argument(&#10;        &quot;--config&quot;,&#10;        default=str(Path(__file__).resolve().with_name(&quot;config.txt&quot;)),&#10;        help=&quot;Path to the training config file.&quot;,&#10;    )&#10;&#10;    # &quot;rho&quot; command - train specific rho values (with optional dataset)&#10;    rho_parser = subparsers.add_parser(&quot;rho&quot;, help=&quot;Train one or more rho-specific models.&quot;)&#10;    rho_parser.add_argument(&quot;rho_values&quot;, nargs=&quot;+&quot;, type=int, help=&quot;rho values to train, e.g. 256 266 276&quot;)&#10;    rho_parser.add_argument(&#10;        &quot;--dataset&quot;,&#10;        type=str,&#10;        default=None,&#10;        help=&quot;Dataset ID (optional). If provided, data will be loaded from data/{dataset_id}/&quot;,&#10;    )&#10;    rho_parser.add_argument(&#10;        &quot;--config&quot;,&#10;        default=str(Path(__file__).resolve().with_name(&quot;config.txt&quot;)),&#10;        help=&quot;Path to the training config file.&quot;,&#10;    )&#10;    &#10;    args = parser.parse_args()&#10;    &#10;    # For dataset command, set rho_values from the --rho argument&#10;    if args.command == &quot;dataset&quot;:&#10;        args.rho_values = args.rho&#10;    &#10;    return args&#10;&#10;&#10;if __name__ == &quot;__main__&quot;:&#10;    try:&#10;        main()&#10;    finally:&#10;        cleanup_bytecode_caches(PROJECT_ROOT)&#10;" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/train/train.py">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/train/train.py" />
              <option name="originalContent" value="from __future__ import annotations&#10;&#10;import copy&#10;import math&#10;import random&#10;import time&#10;from collections.abc import Callable&#10;&#10;import criterion&#10;import numpy as np&#10;import torch&#10;import torch.nn as nn&#10;&#10;from .config import OptimizerConfig, TrainingRuntimeConfig&#10;&#10;&#10;#set the random seed for reproducibility across random, numpy, and torch ( CPU and CUDA).&#10;def set_all_seeds(seed: int) -&gt; None:&#10;    random.seed(seed)&#10;    np.random.seed(seed)&#10;    torch.manual_seed(seed)&#10;    if torch.cuda.is_available():&#10;        torch.cuda.manual_seed_all(seed)&#10;&#10;&#10;def train_one_epoch(&#10;    model: nn.Module, #through the defined model in model_structure&#10;    loader,&#10;    optimizer: torch.optim.Optimizer,  #adam&#10;    criterion: nn.Module,     # the forward&#10;    device: torch.device,&#10;) -&gt; dict[str, float]:    # find the model for different rho&#10;    model.train()   # start train&#10;&#10;    total_loss = 0.0&#10;    total_mae = 0.0&#10;    total_count = 0&#10;    max_ae = 0.0&#10;&#10;'''&#10;to device cuda, and non_blocking=True means that the data transfer can be &#10;asynchronous with respect to the host, which can improve performance when using pinned memory.&#10;'''&#10;    for x_batch, y_batch in loader:&#10;        x_batch = x_batch.to(device, non_blocking=True)&#10;        y_batch = y_batch.to(device, non_blocking=True)&#10;&#10;        predictions = model(x_batch)&#10;        if predictions.shape != y_batch.shape:&#10;            y_batch = y_batch.view_as(predictions)&#10;&#10;        loss = criterion(predictions, y_batch)&#10;&#10;        optimizer.zero_grad(set_to_none=True)&#10;        loss.backward()&#10;        optimizer.step()&#10;&#10;        batch_ae = torch.abs(predictions - y_batch)&#10;        max_ae = max(max_ae, torch.max(batch_ae).item())&#10;&#10;        batch_size = x_batch.size(0)&#10;        total_loss += loss.item() * batch_size&#10;        total_mae += batch_ae.mean().item() * batch_size&#10;        total_count += batch_size&#10;&#10;    mse = total_loss / total_count&#10;    rmse = math.sqrt(mse)&#10;    mae = total_mae / total_count&#10;    return {&quot;mse&quot;: mse, &quot;rmse&quot;: rmse, &quot;mae&quot;: mae, &quot;max_ae&quot;: max_ae}&#10;&#10;&#10;@torch.no_grad()&#10;def evaluate(model: nn.Module, loader, criterion: nn.Module, device: torch.device) -&gt; dict[str, float]:&#10;    model.eval()&#10;&#10;    total_loss = 0.0&#10;    total_mae = 0.0&#10;    total_count = 0&#10;    max_ae = 0.0&#10;&#10;    for x_batch, y_batch in loader:&#10;        x_batch = x_batch.to(device, non_blocking=True)&#10;        y_batch = y_batch.to(device, non_blocking=True)&#10;&#10;        predictions = model(x_batch)&#10;        if predictions.shape != y_batch.shape:&#10;            y_batch = y_batch.view_as(predictions)&#10;&#10;        loss = criterion(predictions, y_batch)&#10;        batch_ae = torch.abs(predictions - y_batch)&#10;        max_ae = max(max_ae, torch.max(batch_ae).item())&#10;&#10;        batch_size = x_batch.size(0)&#10;        total_loss += loss.item() * batch_size&#10;        total_mae += batch_ae.mean().item() * batch_size&#10;        total_count += batch_size&#10;&#10;    mse = total_loss / total_count&#10;    rmse = math.sqrt(mse)&#10;    mae = total_mae / total_count&#10;    return {&quot;mse&quot;: mse, &quot;rmse&quot;: rmse, &quot;mae&quot;: mae, &quot;max_ae&quot;: max_ae}&#10;&#10;&#10;def fit_regression_model(&#10;    rho: int,&#10;    model: nn.Module,&#10;    train_loader,&#10;    val_loader,&#10;    test_loader,&#10;    *,&#10;    optimizer_config: OptimizerConfig,&#10;    training_config: TrainingRuntimeConfig,&#10;    device: torch.device,&#10;    save_path,&#10;    log_message: Callable[[str], None] | None = None,&#10;) -&gt; dict[str, float | int]:&#10;    model = model.to(device)&#10;    criterion = nn.MSELoss()   #the loss is mse&#10;    optimizer = torch.optim.Adam(&#10;        model.parameters(),&#10;        lr=optimizer_config.lr,&#10;        betas=optimizer_config.betas,&#10;        eps=optimizer_config.eps,&#10;        weight_decay=optimizer_config.weight_decay,&#10;    )&#10;&#10;    best_val_mae = float(&quot;inf&quot;)&#10;    best_epoch = -1&#10;    best_state = copy.deepcopy(model.state_dict())&#10;    epochs_without_improvement = 0&#10;    started_at = time.time()&#10;&#10;    def emit(message: str) -&gt; None:&#10;        print(message)&#10;        if log_message is not None:&#10;            log_message(message)&#10;&#10;    for epoch in range(1, training_config.max_epochs + 1):&#10;        train_metrics = train_one_epoch(model, train_loader, optimizer,criterion, device)&#10;        val_metrics = evaluate(model, val_loader, criterion, device)&#10;        test_metrics = evaluate(model, test_loader, criterion, device)&#10;&#10;        improved = val_metrics[&quot;mae&quot;] &lt; best_val_mae&#10;        if improved:&#10;            best_val_mae = val_metrics[&quot;mae&quot;]&#10;            best_epoch = epoch&#10;            best_state = copy.deepcopy(model.state_dict())&#10;            torch.save(best_state, save_path)&#10;            epochs_without_improvement = 0&#10;        else:&#10;            epochs_without_improvement += 1&#10;&#10;        marker = &quot; &lt;- best&quot; if improved else &quot;&quot;&#10;        current_lr = optimizer.param_groups[0][&quot;lr&quot;]&#10;        emit(&#10;            f&quot;[rho={rho}][Epoch {epoch:03d}/{training_config.max_epochs}] &quot;&#10;            f&quot;LR={current_lr:.3e} | &quot;&#10;            f&quot;Train: MSE={train_metrics['mse']:.6e}, RMSE={train_metrics['rmse']:.6e}, &quot;&#10;            f&quot;MAE={train_metrics['mae']:.6e}, MaxAE={train_metrics['max_ae']:.6e} | &quot;&#10;            f&quot;Val: MSE={val_metrics['mse']:.6e}, RMSE={val_metrics['rmse']:.6e}, &quot;&#10;            f&quot;MAE={val_metrics['mae']:.6e}, MaxAE={val_metrics['max_ae']:.6e} | &quot;&#10;            f&quot;Test: MSE={test_metrics['mse']:.6e}, RMSE={test_metrics['rmse']:.6e}, &quot;&#10;            f&quot;MAE={test_metrics['mae']:.6e}, MaxAE={test_metrics['max_ae']:.6e}&quot;&#10;            f&quot;{marker}&quot;&#10;        )&#10;&#10;        if epochs_without_improvement &gt;= training_config.patience:&#10;            emit(&#10;                f&quot;[rho={rho}] Early stopping triggered at epoch {epoch} &quot;&#10;                f&quot;(patience={training_config.patience}).&quot;&#10;            )&#10;            break&#10;&#10;    elapsed_seconds = time.time() - started_at&#10;    model.load_state_dict(best_state)&#10;    test_metrics = evaluate(model, test_loader, criterion, device)&#10;&#10;    emit(f&quot;[rho={rho}] Training finished in {elapsed_seconds:.2f}s&quot;)&#10;    emit(f&quot;[rho={rho}] Best epoch: {best_epoch}&quot;)&#10;    emit(f&quot;[rho={rho}] Best val MAE: {best_val_mae:.6e}&quot;)&#10;    emit(&#10;        f&quot;[rho={rho}] Test MSE={test_metrics['mse']:.6e} | &quot;&#10;        f&quot;Test MAE={test_metrics['mae']:.6e} | &quot;&#10;        f&quot;Test MaxAE={test_metrics['max_ae']:.6e}&quot;&#10;    )&#10;&#10;    return {&#10;        &quot;rho&quot;: rho,&#10;        &quot;best_epoch&quot;: best_epoch,&#10;        &quot;best_val_mae&quot;: best_val_mae,&#10;        &quot;test_mse&quot;: test_metrics[&quot;mse&quot;],&#10;        &quot;test_mae&quot;: test_metrics[&quot;mae&quot;],&#10;        &quot;test_max_ae&quot;: test_metrics[&quot;max_ae&quot;],&#10;        &quot;elapsed_seconds&quot;: elapsed_seconds,&#10;    }&#10;" />
              <option name="updatedContent" value="from __future__ import annotations&#10;&#10;import copy&#10;import math&#10;import random&#10;import time&#10;from collections.abc import Callable&#10;&#10;import criterion&#10;import numpy as np&#10;import torch&#10;import torch.nn as nn&#10;&#10;from .config import OptimizerConfig, TrainingRuntimeConfig&#10;&#10;&#10;#set the random seed for reproducibility across random, numpy, and torch ( CPU and CUDA).&#10;def set_all_seeds(seed: int) -&gt; None:&#10;    random.seed(seed)&#10;    np.random.seed(seed)&#10;    torch.manual_seed(seed)&#10;    if torch.cuda.is_available():&#10;        torch.cuda.manual_seed_all(seed)&#10;&#10;&#10;def train_one_epoch(&#10;    model: nn.Module, #through the defined model in model_structure&#10;    loader,&#10;    optimizer: torch.optim.Optimizer,  #adam&#10;    criterion: nn.Module,     # the forward&#10;    device: torch.device,&#10;) -&gt; dict[str, float]:    # find the model for different rho&#10;    model.train()   # start train&#10;&#10;    total_loss = 0.0&#10;    total_mae = 0.0&#10;    total_count = 0&#10;    max_ae = 0.0&#10;&#10;'''&#10;to device cuda, and non_blocking=True means that the data transfer can be &#10;asynchronous with respect to the host, which can improve performance when using pinned memory.&#10;'''&#10;    for x_batch, y_batch in loader:&#10;        x_batch = x_batch.to(device, non_blocking=True)&#10;        y_batch = y_batch.to(device, non_blocking=True)&#10;&#10;        predictions = model(x_batch)&#10;        if predictions.shape != y_batch.shape:&#10;            y_batch = y_batch.view_as(predictions)&#10;&#10;        loss = criterion(predictions, y_batch)&#10;&#10;        optimizer.zero_grad(set_to_none=True)&#10;        loss.backward()&#10;        optimizer.step()&#10;&#10;        batch_ae = torch.abs(predictions - y_batch)&#10;        max_ae = max(max_ae, torch.max(batch_ae).item())&#10;&#10;        batch_size = x_batch.size(0)&#10;        total_loss += loss.item() * batch_size&#10;        total_mae += batch_ae.mean().item() * batch_size&#10;        total_count += batch_size&#10;&#10;    mse = total_loss / total_count&#10;    rmse = math.sqrt(mse)&#10;    mae = total_mae / total_count&#10;    return {&quot;mse&quot;: mse, &quot;rmse&quot;: rmse, &quot;mae&quot;: mae, &quot;max_ae&quot;: max_ae}&#10;&#10;&#10;@torch.no_grad()&#10;def evaluate(model: nn.Module, loader, criterion: nn.Module, device: torch.device) -&gt; dict[str, float]:&#10;    model.eval()&#10;&#10;    total_loss = 0.0&#10;    total_mae = 0.0&#10;    total_count = 0&#10;    max_ae = 0.0&#10;&#10;    for x_batch, y_batch in loader:&#10;        x_batch = x_batch.to(device, non_blocking=True)&#10;        y_batch = y_batch.to(device, non_blocking=True)&#10;&#10;        predictions = model(x_batch)&#10;        if predictions.shape != y_batch.shape:&#10;            y_batch = y_batch.view_as(predictions)&#10;&#10;        loss = criterion(predictions, y_batch)&#10;        batch_ae = torch.abs(predictions - y_batch)&#10;        max_ae = max(max_ae, torch.max(batch_ae).item())&#10;&#10;        batch_size = x_batch.size(0)&#10;        total_loss += loss.item() * batch_size&#10;        total_mae += batch_ae.mean().item() * batch_size&#10;        total_count += batch_size&#10;&#10;    mse = total_loss / total_count&#10;    rmse = math.sqrt(mse)&#10;    mae = total_mae / total_count&#10;    return {&quot;mse&quot;: mse, &quot;rmse&quot;: rmse, &quot;mae&quot;: mae, &quot;max_ae&quot;: max_ae}&#10;&#10;&#10;def fit_regression_model(&#10;    rho: int,&#10;    model: nn.Module,&#10;    train_loader,&#10;    val_loader,&#10;    test_loader,&#10;    *,&#10;    optimizer_config: OptimizerConfig,&#10;    training_config: TrainingRuntimeConfig,&#10;    device: torch.device,&#10;    save_path,&#10;    log_message: Callable[[str], None] | None = None,&#10;) -&gt; dict[str, float | int]:&#10;    model = model.to(device)&#10;    criterion = nn.MSELoss()   #the loss is mse&#10;    optimizer = torch.optim.Adam(&#10;        model.parameters(),&#10;        lr=optimizer_config.lr,&#10;        betas=optimizer_config.betas,&#10;        eps=optimizer_config.eps,&#10;        weight_decay=optimizer_config.weight_decay,&#10;    )&#10;&#10;    best_val_mae = float(&quot;inf&quot;)&#10;    best_epoch = -1&#10;    best_state = copy.deepcopy(model.state_dict())&#10;    epochs_without_improvement = 0&#10;    started_at = time.time()&#10;    stop_epoch = None&#10;    stop_info = None&#10;&#10;    def emit(message: str) -&gt; None:&#10;        print(message)&#10;        if log_message is not None:&#10;            log_message(message)&#10;&#10;    for epoch in range(1, training_config.max_epochs + 1):&#10;        train_metrics = train_one_epoch(model, train_loader, optimizer,criterion, device)&#10;        val_metrics = evaluate(model, val_loader, criterion, device)&#10;        test_metrics = evaluate(model, test_loader, criterion, device)&#10;&#10;        improved = val_metrics[&quot;mae&quot;] &lt; best_val_mae&#10;        if improved:&#10;            best_val_mae = val_metrics[&quot;mae&quot;]&#10;            best_epoch = epoch&#10;            best_state = copy.deepcopy(model.state_dict())&#10;            torch.save(best_state, save_path)&#10;            epochs_without_improvement = 0&#10;        else:&#10;            epochs_without_improvement += 1&#10;&#10;        marker = &quot; &lt;- best&quot; if improved else &quot;&quot;&#10;        current_lr = optimizer.param_groups[0][&quot;lr&quot;]&#10;        emit(&#10;            f&quot;[rho={rho}][Epoch {epoch:03d}/{training_config.max_epochs}] &quot;&#10;            f&quot;LR={current_lr:.3e} | &quot;&#10;            f&quot;Train: MSE={train_metrics['mse']:.6e}, RMSE={train_metrics['rmse']:.6e}, &quot;&#10;            f&quot;MAE={train_metrics['mae']:.6e}, MaxAE={train_metrics['max_ae']:.6e} | &quot;&#10;            f&quot;Val: MSE={val_metrics['mse']:.6e}, RMSE={val_metrics['rmse']:.6e}, &quot;&#10;            f&quot;MAE={val_metrics['mae']:.6e}, MaxAE={val_metrics['max_ae']:.6e} | &quot;&#10;            f&quot;Test: MSE={test_metrics['mse']:.6e}, RMSE={test_metrics['rmse']:.6e}, &quot;&#10;            f&quot;MAE={test_metrics['mae']:.6e}, MaxAE={test_metrics['max_ae']:.6e}&quot;&#10;            f&quot;{marker}&quot;&#10;        )&#10;&#10;        if epochs_without_improvement &gt;= training_config.patience:&#10;            emit(&#10;                f&quot;[rho={rho}] Early stopping triggered at epoch {epoch} &quot;&#10;                f&quot;(patience={training_config.patience}).&quot;&#10;            )&#10;            stop_epoch = epoch&#10;            stop_info = (&#10;                f&quot;Early stopping at epoch {stop_epoch}: &quot;&#10;                f&quot;LR={current_lr:.3e} | &quot;&#10;                f&quot;Train: MSE={train_metrics['mse']:.6e}, RMSE={train_metrics['rmse']:.6e}, &quot;&#10;                f&quot;MAE={train_metrics['mae']:.6e}, MaxAE={train_metrics['max_ae']:.6e} | &quot;&#10;                f&quot;Val: MSE={val_metrics['mse']:.6e}, RMSE={val_metrics['rmse']:.6e}, &quot;&#10;                f&quot;MAE={val_metrics['mae']:.6e}, MaxAE={val_metrics['max_ae']:.6e} | &quot;&#10;                f&quot;Test: MSE={test_metrics['mse']:.6e}, RMSE={test_metrics['rmse']:.6e}, &quot;&#10;                f&quot;MAE={test_metrics['mae']:.6e}, MaxAE={test_metrics['max_ae']:.6e}&quot;&#10;            )&#10;            break&#10;&#10;    elapsed_seconds = time.time() - started_at&#10;    model.load_state_dict(best_state)&#10;    test_metrics = evaluate(model, test_loader, criterion, device)&#10;&#10;    emit(f&quot;[rho={rho}] Training finished in {elapsed_seconds:.2f}s&quot;)&#10;    emit(f&quot;[rho={rho}] Best epoch: {best_epoch}&quot;)&#10;    emit(f&quot;[rho={rho}] Best val MAE: {best_val_mae:.6e}&quot;)&#10;    emit(&#10;        f&quot;[rho={rho}] Test MSE={test_metrics['mse']:.6e} | &quot;&#10;        f&quot;Test MAE={test_metrics['mae']:.6e} | &quot;&#10;        f&quot;Test MaxAE={test_metrics['max_ae']:.6e}&quot;&#10;    )&#10;&#10;    return {&#10;        &quot;rho&quot;: rho,&#10;        &quot;best_epoch&quot;: best_epoch,&#10;        &quot;best_val_mae&quot;: best_val_mae,&#10;        &quot;test_mse&quot;: test_metrics[&quot;mse&quot;],&#10;        &quot;test_mae&quot;: test_metrics[&quot;mae&quot;],&#10;        &quot;test_max_ae&quot;: test_metrics[&quot;max_ae&quot;],&#10;        &quot;elapsed_seconds&quot;: elapsed_seconds,&#10;        &quot;stop_epoch&quot;: stop_epoch,&#10;        &quot;stop_info&quot;: stop_info,&#10;    }&#10;" />
            </PendingDiffInfo>
          </value>
        </entry>
      </map>
    </option>
  </component>
</project>